apiVersion: v1
kind: Service
metadata:
  name: jenkins
spec:
  selector:
    app: jenkins
  type: ClusterIP
  ports:
    - name: jenkins
      port: 80
      protocol: TCP
      targetPort: 8080
    - name: jnlpport
      port: 50000
      protocol: TCP
      targetPort: 50000